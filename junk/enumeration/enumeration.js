(function() {
    'use strict';

    /**
     * @ngdoc overview
     * @name jchptf.tools.enumeration
     * @description
     *
     * Utility module encapsulating the coffee Enum base class.
     *
     * NOT USED because Enum subtypes should be bundled as constants and so cannot get
     * their Enum base class injected from another module!
     */
    this.angular.module('jchptf.tools.enumeration', []);
}());
