'use strict';

describe('Service: jchptf.site.context.AuthService.Factory', function () {

    // load the service's module
    beforeEach(module('jchptf.site.context'));

    // instantiate service
    var service;

    //update the injection
    beforeEach(inject(function (_AuthService.Factory_) {
        service = _AuthService.Factory_;
    }));

    /**
     * @description
     * Sample test case to check if the service is injected properly
     * */
    it('should be injected and defined', function () {
        expect(service).toBeDefined();
    });
});
